// http://json2ts.com/ - generator modelu

  export interface Translation {
      pl: string;
      en: string;
  }

  export interface Translations {
      test: string;
      translations: Translation[];
  }

